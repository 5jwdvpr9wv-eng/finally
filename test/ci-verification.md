# CI Verification

Temporary file to trigger `claude-code-review.yml` and confirm the Claude
GitHub App is wired up correctly with `CLAUDE_CODE_OAUTH_TOKEN`.

Safe to delete once the workflow run is confirmed.
