services:
  finally:
    build: .
    image: finally
    container_name: finally
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ./db:/app/db
    env_file:
      - .env
    restart: unless-stopped
